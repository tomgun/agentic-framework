# Acceptance Criteria: F-0112 - Code Style Examples in CONTEXT_PACK

## Overview
Add Code Style Examples section to CONTEXT_PACK template so agents can mimic project patterns.

## Acceptance Criteria

- [x] CONTEXT_PACK.template.md has "Code style examples" section
- [x] Uses pseudocode (language-agnostic) with note to customize
- [x] Includes maintenance guidance comments (when to update, signs of staleness)
- [x] Shows alternative: referencing real files instead of inline
- [x] Section placed logically (after Architecture, before Quality gates)
- [x] Examples cover: function style, error handling, test structure

## Validation

```bash
# Check section exists
grep -A 5 "Code style examples" .agentic/init/CONTEXT_PACK.template.md

# Check maintenance guidance
grep -i "maintenance\|stale\|update" .agentic/init/CONTEXT_PACK.template.md
```
