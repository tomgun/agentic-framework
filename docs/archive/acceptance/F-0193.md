# F-0193: Centralized & Future-Proof Feature IDs — Acceptance Criteria

## P1 — Must Have

- [x] **AC-001**: Central Python module (`.agentic/lib/ids.py`) provides `FEATURE_ID_RE`, `FEATURE_HEADER_RE`, `FEATURE_ID_STRICT_RE`, `is_valid_feature_id()`, `format_feature_id()`, `get_next_feature_id()`
- [x] **AC-002**: Central shell module (`.agentic/lib/ids.sh`) provides `$FEATURE_ID_ERE`, `$FEATURE_ID_ERE_ANCHORED`, `$FEATURE_HEADER_ERE`, `is_feature_id()`, `format_feature_id()`
- [x] **AC-003**: All Python files in `.agentic/lib/` import feature ID patterns from `ids.py` instead of defining their own — no hardcoded `F-\d{4}` (4-digit-only) patterns remain
- [x] **AC-004**: All shell files in `.agentic/lib/` use centralized patterns from `ids.sh` — no hardcoded `F-[0-9]{4}` (4-digit-only) patterns remain
- [x] **AC-005**: Patterns widened from `\d{4}` to `\d{4,}` — IDs F-10000+ are accepted by all validators, parsers, and schemas
- [x] **AC-006**: JSON schemas (`feature.schema.json`, `nfr.schema.json`) updated to accept 4+ digit feature IDs
- [x] **AC-007**: `get_next_feature_id()` moved from `epic.py` to `ids.py`; callers import from `ids` directly (no re-export)
- [x] **AC-008**: ID generation callsites use `format_feature_id()` instead of inline `f"F-{n:04d}"` / `printf "F-%04d"`

## P2 — Should Have

- [x] **AC-009**: Unit tests (`tests/test_ids.py`) cover format, validation, patterns, and allocation
- [x] **AC-010**: Framework validation (`validate_framework.sh`) includes smoke tests for centralized IDs
- [x] **AC-011**: Existing tests (epic, kickoff, review) pass without regressions
- [x] **AC-012**: Instruction files updated (memory-seed.md, DEVELOPER_GUIDE.md) documenting the centralized modules

## Verification

**Automated**: `python3 -m pytest tests/test_ids.py && bash tests/validate_framework.sh`
