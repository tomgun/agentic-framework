# F-0124: Domain Categories & Systematic Brownfield Spec Generation

## Acceptance Criteria

### Domain Detection
- [x] `detect_infra_patterns()` scans for CI/CD, IaC, containers, deployment configs
- [x] `detect_domains()` groups sub-projects by framework type (frontend, backend, mobile, etc.)
- [x] Infrastructure domain created when ≥3 infra patterns or dedicated IaC directory exists
- [x] Single-project repos get 1 domain named from repo/package name
- [x] Clusters assigned to domains by path prefix match
- [x] Report always includes `domains` and `infra_patterns` fields

### Domain-Tagged Output
- [x] `render_proposals.py` adds `- Domain:` metadata to each feature
- [x] Feature heading format `## F-XXXX:` unchanged (no breaking changes)
- [x] Auto-increment IDs sequentially across all domains

### `ag specs` Command
- [x] Requires Formal profile
- [x] Checks for discovery report (suggests running discover.py if missing)
- [x] Detects existing plan (APPROVED/DRAFT/REVISION_NEEDED) and prints appropriate instructions
- [x] Prints domain summary from discovery report
- [x] `ag specs --status` shows domain completion progress

### Size-Aware Routing
- [x] Small (1 domain AND ≤8 clusters) → inline spec generation
- [x] Large (>1 domain OR >8 clusters) → suggests `ag specs`

### Multi-Session Support
- [x] Plan artifact uses checkbox format for domain completion tracking
- [x] Session start detects active brownfield plan and suggests resuming
- [x] Brownfield Spec Pipeline documented in auto_orchestration.md

### Greenfield Support
- [x] Init interview asks Formal projects about distinct domains

### Tests
- [x] 16 new pytest tests (7 infra, 8 domains, 1 integration update)
- [x] 3 new LLM behavioral tests (044-046)
- [x] All 75 discover tests pass
- [x] 184/184 framework validation checks pass
