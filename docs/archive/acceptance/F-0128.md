# F-0128: Specs-Before-Code Structural Enforcement

## Acceptance Criteria

### Root Cause Analysis
- [x] Documented why current instructions fail to enforce specs-first (introspect CLAUDE.md, memory-seed.md, ag.sh, auto_orchestration.md)
- [x] Identified all enforcement points that should catch specs-before-code violations
- [x] Categorized gaps as structural (can be gated) vs behavioral (relies on agent compliance)

### Structural Gates
- [x] `ag work` / `ag implement` checks for acceptance criteria existence before allowing work to start
- [x] Tools that start implementation emit a clear warning if no acceptance file exists for the feature
- [x] Commit gates warn when new code files are committed without corresponding spec/acceptance files
- [x] `ag implement` blocks when `plan_review_enabled: yes` and no approved plan exists (prevents skipping `ag plan`)

### Instruction Fixes
- [x] Memory-seed trigger rules explicitly include "write acceptance criteria FIRST" in the build/implement action
- [x] CLAUDE.md trigger table actions reference acceptance criteria creation, not just `ag plan`
- [x] Auto-orchestration documentation reinforces specs-first at each workflow entry point

### General Tool Audit
- [x] All `ag` commands that start work (work, implement, plan) checked for specs-first enforcement
- [x] ~~Session start greeting reminds about specs-first when Formal profile active~~ REMOVED: Structural gates (ag work hard block, ag implement blocking doctor check, pre-commit workflow bypass detection) make session reminders redundant. Adding per-session text reminders creates noise without enforcement value — the exact failure mode this feature addresses.
- [x] Pre-commit gates detect new implementation files without matching acceptance criteria

### Tests
- [x] Framework validation checks pass (184+)
- [x] At least 1 new LLM behavioral test verifying specs-before-code enforcement
