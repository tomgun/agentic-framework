# F-0242: Simulation Testing (Phase + Sequence)

## Acceptance Criteria

### P1 — Core

- [x] AC-1: PhaseChecker parses framework.log (pipe-delimited TIMESTAMP|SCRIPT|VERB|ARGS|RESULT), matches entries by script+verb substrings, handles missing/empty/malformed log gracefully
- [x] AC-2: PhaseChecker validates filesystem state at detected phases — files_exist (glob) and file_contains (path + regex pattern)
- [x] AC-3: `detect_violations()` loads violation metadata (names, descriptions, allowlists, thresholds) from `violations.yaml` via `load_violations_yaml()`
- [x] AC-4: All 3 existing violation patterns (stopped_after_plan_exit, code_before_review, skipped_planning) produce identical output after YAML refactor — verified by golden-fixture comparison test
- [x] AC-5: Scenario YAML `phase_expectations` key consumed by PhaseChecker in `run_scenario()` — returns MilestoneResult entries for each phase
- [x] AC-8: JSONL session log discovered via `discover_jsonl()` and analyzed by `session-analyze.py` in `run_scenario()` — violations surface as failing MilestoneResults

### P2 — Integration

- [x] AC-6: All existing `test_session_analyze.py` tests pass without modification after refactor
- [x] AC-7: `todo_app.yaml` has `phase_expectations` with kickoff and implement phase checks
