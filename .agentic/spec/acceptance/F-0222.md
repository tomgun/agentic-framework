# F-0222: State Machine Enforcement = Blocking

## Acceptance Criteria

- [ ] AC1: When `state_enforcement: blocking`, gate failures block transitions (exit 1)
- [ ] AC2: Discovery profile remains `state_enforcement: off` (unchanged)
- [ ] AC3: SKIP_TRANSITIONS (planned→implementing, planned→shipped) still work in blocking mode
- [ ] AC4: Error messages show the gate failure reason and bypass hint
- [ ] AC5: Existing features in inconsistent states are not retroactively broken

## Verification

- **Automated**:
  - `pytest tests/test_state_machine.py::TestBlockingEnforcement -v` — AC1, AC3, AC4, AC5
  - `profiles.conf` has `formal.state_enforcement=blocking`, `autonomous_formal.state_enforcement=blocking`, `discovery.state_enforcement=off` — AC1, AC2
  - LLM test `079_state_enforcement_blocking` — agent understands blocking behavior
- **Manual**: Set `state_enforcement: blocking`, run `ag implement` on a feature missing AC file, confirm it blocks with clear message
- **Profile**: Formal, Autonomous Formal

## Dependencies

- State machine (ADR-001) already has `enforce` parameter
- Gates (`.agentic/lib/auto/gates.py`) already produce user-friendly messages
