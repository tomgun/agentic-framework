# F-0224: Smoke Test Evidence Gate

## Acceptance Criteria

- [ ] AC1: `ag done F-XXXX` checks for `.agentic/journal/evidence/F-XXXX-smoke.*` when `smoke_test_evidence` != `off`
- [ ] AC2: `smoke_test_evidence: required` blocks `ag done` (exit 1) if no evidence file exists; `recommended` warns but proceeds
- [ ] AC3: Non-interactive bypass: `SKIP_SMOKE_EVIDENCE=1` or `[ ! -t 0 ]` skips the blocking gate in `required` mode
- [ ] AC4: `smoke_test_evidence` setting added to all 3 profiles in `profiles.conf` with defaults: discovery=off, formal=recommended, autonomous_formal=recommended
- [ ] AC5: `EVIDENCE_DIR` / `evidence_dir` added to `paths.sh` and `paths.py` with legacy fallback paths
- [ ] AC6: `ag set smoke_test_evidence <value>` validates against `off|recommended|required`; rejects invalid values
- [ ] AC7: `ag auto verify --visual --feature F-XXXX` saves structured markdown evidence to `.agentic/journal/evidence/F-XXXX-smoke.md`
- [ ] AC8: `ag auto verify --feature F-XXXX` (without `--visual`) prints warning to stderr that no evidence will be saved
- [ ] AC9: STACK.template.md includes the new setting with profile default comment
- [ ] AC10: All instruction files updated: auto_orchestration.md (Enforced Gates table + commands table), agent_operating_guidelines.md (Gates table + quick commands), memory-seed.md, CLAUDE.md template, HOW_IT_WORKS.md (D2 table + Escape Hatches), DEVELOPER_GUIDE.md, copilot-instructions.md, codex-instructions.md
- [ ] AC11: Checklists updated: feature_complete.md references evidence artifacts, smoke_testing.md has "Capturing Evidence" section
- [ ] AC12: `validate_framework.sh` includes F-0224 checks; LLM test 083 exists with test_definitions.json entry

## Verification

- **Automated**: `bash tests/validate_framework.sh` (profiles, paths, gate presence)
- **Automated**: LLM test 083
- **Manual**: Set `smoke_test_evidence: required`, run `ag done F-TEST` without evidence → blocked. Create evidence → passes.
- **Manual**: `ag set smoke_test_evidence junk` → rejected
