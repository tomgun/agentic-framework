# F-0190: Backlog/Roadmap — Structural Work Assignment

Git-tracked ordered work queue that tells any agent on any machine what to work on next.

## Acceptance Criteria

### P1: Core backlog operations

- [ ] **AC-01**: `backlog.sh add F-XXXX` creates BACKLOG.json with item at end of queue; validates feature exists in FEATURES.md
- [ ] **AC-02**: `backlog.sh add F-XXXX --position 0` places item at top of queue; sets `became_current_at`
- [ ] **AC-03**: `backlog.sh add --task "description"` adds non-feature task item
- [ ] **AC-04**: `backlog.sh current` prints position 0 item; exits 1 if backlog is empty
- [ ] **AC-05**: `backlog.sh next` prints position 1 item
- [ ] **AC-06**: `backlog.sh done` removes position 0; sets `became_current_at` on new position 0
- [ ] **AC-07**: `backlog.sh list` prints full backlog with positions and dependency info
- [ ] **AC-08**: `backlog.sh remove F-XXXX` removes specific item by ID
- [ ] **AC-09**: `backlog.sh move F-XXXX N` moves item to position N
- [ ] **AC-10**: `backlog.sh clear` empties the backlog
- [ ] **AC-11**: Auto-discovers plan file and acceptance criteria as refs when adding feature items

### P1: Structural gates

- [ ] **AC-12**: `ag start` shows current backlog item prominently with notes, refs, and next item
- [ ] **AC-13**: `ag implement F-XXXX` auto-adds F-XXXX to backlog at position 0 if not present
- [ ] **AC-14**: `ag implement F-XXXX` where F-XXXX != position 0 exits with BLOCKED (exit 1)
- [ ] **AC-15**: `ag implement F-XXXX` where F-XXXX == position 0 proceeds normally
- [ ] **AC-16**: `ag work "desc"` with feature items in backlog AND feature_tracking=yes exits BLOCKED
- [ ] **AC-17**: `ag done F-XXXX` auto-advances backlog (removes position 0, sets `became_current_at` on next)
- [ ] **AC-18**: `SKIP_BACKLOG=1 ag implement F-XXXX` bypasses backlog gate with warning

### P2: Integration

- [ ] **AC-19**: `ag plan F-YYYY` shows advisory warning if F-YYYY != position 0
- [ ] **AC-20**: `ag spec F-YYYY` shows advisory warning if F-YYYY != position 0
- [ ] **AC-21**: `ag backlog` top-level command works with all subcommands (list, add, done, move, remove, clear)
- [ ] **AC-22**: Staleness detection: `ag start` warns if current item's `became_current_at` > 7 days old
- [ ] **AC-23**: Dependency tracking: items can declare `depends_on` array; advisory warning shown at implement time
- [ ] **AC-24**: Empty backlog or no BACKLOG.json: all commands work normally with no constraints
- [ ] **AC-25**: `backlog.sh done` when next item has unmet deps skips to first unblocked or warns
- [ ] **AC-26**: `ag implement F-XXXX` where F-XXXX is `shipped`/`deprecated` shows lifecycle warning
- [ ] **AC-27**: `crunch.py._read_planned_features()` uses backlog order when BACKLOG.json exists
- [ ] **AC-28**: `BACKLOG_FILE` path defined in both `paths.sh` and `paths.py`
