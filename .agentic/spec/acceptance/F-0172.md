# F-0172: Change Propagation Pipeline — Acceptance Criteria

## Acceptance Criteria

- AC-001: `spec-audit.sh --propagate NFR-XXXX` traces NFR references to affected features
- AC-002: `spec-audit.sh --propagate migration:NNN` traces migration to mentioned features
- AC-003: Propagation report identifies gaps (missing NFR Compliance, stale thresholds)
- AC-004: Propagation results are actionable (specific files, specific changes needed)

## Tests

- `tests/validate_framework.sh`: spec-audit.sh --propagate accepts NFR and migration targets

## NFR Compliance

NFRs: none

- AC-005: `migration.sh create` auto-calls `qa-tracker.sh add-propagation` for affected features
- AC-006: `nfr.sh` status update auto-calls `qa-tracker.sh add-propagation` for referencing features

## Out of Scope

- Automatic remediation (generates plan only)
