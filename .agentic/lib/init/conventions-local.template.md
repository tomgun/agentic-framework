# Project-Specific Conventions

These conventions supplement the framework defaults in `.agentic/conventions.md`.
Rules here take precedence when they conflict with framework defaults.

<!-- Uncomment and customize sections relevant to your project -->

<!-- ## Security overrides
- All API endpoints must validate JWT tokens before processing
- Database connections must use TLS in all environments
-->

<!-- ## Naming overrides
- Use snake_case for all Python files and functions
- Prefix all internal API routes with `/internal/`
-->

<!-- ## Code structure overrides
- Maximum function length: 30 lines (stricter than framework default of 50)
- Use Result<T, E> types instead of throwing exceptions
-->

<!-- ## Testing overrides
- Integration tests must hit a real database, not mocks
- Minimum test coverage: 80% for new code
-->

<!-- ## Custom rules
Add project-specific rules that don't fit the categories above.
- All PRs must include a changelog entry
- Use 2-space indentation for TypeScript, 4-space for Python
-->
