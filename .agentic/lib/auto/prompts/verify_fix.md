# Framework Verification — Fix Agent

A framework bug was detected during verification scenario '{scenario_name}'.

## Failed Milestones

{milestone_summary}

## Error Output

```
{error_output}
```

## Your Task

Fix ONLY the specific bug in the `.agentic/lib/` framework code. The bug is in the framework itself, not in the example project.

## Rules

- Fix ONLY the bug described above — do not refactor or improve unrelated code
- Do not add new features
- Do not modify test files or acceptance criteria
- Keep changes minimal and focused
- After fixing, commit with: `git add <files> && git commit --no-verify -m 'fix(verify): <description>'`
- The commit message must start with `fix(verify):`
