# Plan: Structural Doc Update Enforcement (F-0220)

**Status**: APPROVED
**Date**: 2026-03-19
**Feature**: F-0220

## Context

Doc updates keep getting skipped when features ship. Every enforcement point is advisory — agents skip them. This session alone: F-0217, F-0218, F-0219 all shipped without doc updates until the user manually asked. The root cause is architectural: doc updates are reactive (triggered after done) not structural (blocking before done). The existing `docs_gate: blocking` setting has no teeth — it fires a human prompt that autonomous pipelines skip (`! -t 0` escape), and `drift.sh` exit codes are swallowed with `|| true`.

## Approach: Add teeth to existing infrastructure

Three structural changes, plus skill updates for behavioral guidance:

### Revision Notes (from dialectical review)

The following revisions were incorporated from Critic/Advocate review:

1. **[Critic HIGH] Dynamic base branch detection**: Original plan hardcoded `main`. Revised: use `git rev-parse --verify origin/HEAD` with fallback to `main`/`master` detection. Reject literal `HEAD` (no-remote fallback).
2. **[Critic HIGH] On-main handling**: Original `git diff main...HEAD` is empty on main branch. Revised: when current branch == base branch, skip branch diff entirely, rely on recent-commit check only.
3. **[Critic MEDIUM] Gate sequencing**: Original plan placed freshness check in the doc drift section (before doc lifecycle fires). Revised: freshness check runs AFTER doc lifecycle section, saved as Gate 4 in `done_failures` block. This gives agents a chance to update docs before being blocked.
4. **[Critic MEDIUM] --trigger filter**: `--check-freshness` defaults to `--trigger feature_done` filter, so only relevant docs are checked.
5. **[Critic LOW dismissed] 10 feature_done docs**: Acknowledged but not changed — `docs_gate: blocking` is opt-in, and the registry is user-configured. Users who register 10 docs with `feature_done` trigger are declaring they want all 10 checked. They can use `docs_gate: warning` if too strict.

### 1. `docs.sh --check-freshness` — New deterministic freshness check

**File**: `.agentic/lib/tools/docs.sh`

Add `detect_base_branch()` + `check_freshness()` functions:
- `detect_base_branch()`: tries `origin/HEAD`, falls back to `main`/`master` detection, rejects literal `HEAD`
- `check_freshness()`: parses STACK.md `## Docs` registry, filters by trigger (default: `feature_done`)
- For each registered doc, checks if modified in current branch or recent commits
- All git commands use `git -C "$ROOT_DIR"` for correct directory context
- Returns exit 0 if all fresh, exit 1 with stale list if not
- CLI: `docs.sh --check-freshness [--trigger feature_done]`

Freshness logic (deterministic, git-based — no heuristics):
- File doesn't exist → STALE
- File in `git diff <base>...HEAD` (only on feature branches) → FRESH
- File modified in last 5 commits → FRESH
- Otherwise → STALE

### 2. `ag.sh` cmd_done — Replace advisory prompt with `done_failures` gate

**File**: `.agentic/lib/tools/ag.sh` (lines ~1549-1592)

Replace the current doc drift section with a structural gate:
- Remove `! -t 0` escape (autonomous and interactive treated identically)
- Doc drift section becomes advisory only (shows drift, validates registry)
- Freshness check runs AFTER doc lifecycle section (agent gets chance to update docs first)
- Result saved in `docs_freshness_exit`, checked as Gate 4 in `done_failures` block
- If stale + `docs_gate: blocking` → increment `done_failures` (hard exit 1)
- If stale + `docs_gate: warning` → warn only
- `docs_mode: deferred` → skip freshness but still check registry health (missing files = always an error)

### 3. Pre-commit Check 19 — Make it blocking when `docs_gate: blocking`

**File**: `.agentic/lib/hooks/pre-commit-check.sh` (lines ~1057-1078)

Only for registered-but-missing files (not freshness — too expensive at commit time):
- `docs_gate: off` → skip
- `docs_gate: warning` → advisory (current)
- `docs_gate: blocking` → increment FAILURES

### 4. Skills — Add doc verification steps

**Files** (4 files, root + template pairs):
- `completing-work/SKILL.md`: Add Step 5b — run `docs.sh --check-freshness` before `ag done`
- `implementing-features/SKILL.md`: Add doc check to Step 6 verification list

### 5. Tests

**File**: `tests/test_docs_freshness.sh` (new)
- Recently committed doc → exit 0
- Stale doc → exit 1 with list
- No feature_done docs → exit 0
- Missing registered doc → exit 1

## Files Changed (8 total)

| File | Change |
|------|--------|
| `.agentic/lib/tools/docs.sh` | Add `check_freshness()` + `--check-freshness` CLI |
| `.agentic/lib/tools/ag.sh` | Replace advisory doc prompt with `done_failures` gate |
| `.agentic/lib/hooks/pre-commit-check.sh` | Check 19 blocking when `docs_gate: blocking` |
| `.agentic/lib/agents/claude/skills/completing-work/SKILL.md` | Add Step 5b |
| `.claude/skills/completing-work/SKILL.md` | Mirror Step 5b |
| `.agentic/lib/agents/claude/skills/implementing-features/SKILL.md` | Add doc check to Step 6 |
| `.claude/skills/implementing-features/SKILL.md` | Mirror doc check |
| `tests/test_docs_freshness.sh` | New test |

## What this does NOT change

- `docs.sh --trigger` still fires (assembles drafting context)
- `drift.sh --docs` remains advisory for heuristic detection
- `docs_mode: deferred` remains valid — suppresses freshness blocking but not registry health
- Discovery profile unaffected (`docs_gate: off` by default)

## Verification

1. `bash tests/test_docs_freshness.sh` — new tests pass
2. `bash tests/validate_framework.sh` — existing tests pass
3. Manual: `ag done F-XXXX` with stale docs → blocks with clear message
4. Manual: `ag done F-XXXX` with fresh docs → passes
5. Manual: `docs_mode: deferred` + stale docs → warns but doesn't block on freshness
