# Plan: F-0216 — NFR Auto-Generation at Lifecycle Moments

**Status**: DRAFT (revised after dialectical review)
**Feature**: F-0216
**Date**: 2026-03-19

## Context

`ag nfr discover` currently dumps ALL matching catalog entries (8-20+) with agent instructions to manually assign IDs and write to NFR.md. No batch write tool exists. No integration with `ag kickoff`. The user experience is passive — too many choices, no tooling to write them, no lifecycle integration.

Most ACs are already satisfied by existing infrastructure. The remaining 5 ACs need targeted changes to 2 existing scripts + 1 new script.

## Gap Analysis

**Already satisfied (no changes):** AC-001 through AC-004 (generation tool), AC-010 through AC-012 (catalog expansion), AC-013-014 (component scoping).

**Needs implementation:**
- **AC-005**: Present 4-8 pre-selected (not all) recommendations
- **AC-006**: Batch write selected NFRs to NFR.md with collision-free IDs
- **AC-007**: Merge with existing NFRs (append, don't overwrite)
- **AC-008**: `cmd_kickoff` calls `nfr-generate.sh` at ag.sh layer
- **AC-009**: NFR suggestions appear in kickoff staging area

## Implementation

### 1. nfr-generate.sh — Add `--limit` and `--machine` flags

**File**: `.agentic/lib/tools/nfr-generate.sh` (~35 lines changed)

Add two new flags to argument parsing:
- `--limit N`: Cap output at N entries post-filter. Applied after priority/component filtering so users always get the top N most relevant entries, not the first N catalog rows.
- `--machine`: Output one pipe-delimited line per entry (`ID|category|statement|measure|enforced|priority|section`), no header, no footer, no ANSI codes. Essential for piping to `nfr-write-batch.sh`.

**Selection heuristic for `--limit` (revised — no awk sorting):**
Instead of sorting with awk, reorder `map_type_to_sections()` output: type-specific sections FIRST, then "Universal", then "Framework Promises". Since catalog tables already list P1 entries before P2, the natural iteration order matches desired priority. `--limit` is a simple counter in the output loop: `[[ $count -ge $LIMIT ]] && break`. No awk, no sort, fully portable.

**`--machine` implementation:** Guard the header block (lines 198-200), the formatted output loop (lines 220-232), and the footer (lines 234-235) with conditionals. In machine mode, the output loop emits raw pipe-delimited lines instead of formatted blocks.

**`--help` text updated** to document both new flags.

Without these flags, behavior is unchanged (backward compatible).

### 2. nfr-write-batch.sh — New batch writer (~110 lines)

**File**: `.agentic/lib/tools/nfr-write-batch.sh` (CREATE)

Accepts pipe-delimited entries from stdin (one per line, same format as `nfr-generate.sh --machine`).

**Atomic reads:** Read ALL stdin into a variable first, validate line count > 0, then write. Prevents partial writes on pipe errors.

Flow:
1. Read all stdin into variable; exit 1 if empty
2. Multi-agent safety check (reuse `agents_helpers.py count-others` pattern from `nfr-capture.sh:51-59`)
3. Create NFR.md from template if doesn't exist (reuse pattern from `nfr-capture.sh:66-77`)
4. Find max NFR-XXXX ID in existing NFR.md (reuse `nfr-capture.sh:62-65`)
5. Append each entry with incrementing IDs, standard NFR.md format
6. Print summary of written NFRs

**Field mapping (catalog → NFR.md template):**
- `statement` → `## NFR-XXXX: {statement}` + `- Statement: {statement}`
- `category` → `- Category: {category}`
- `section` → `- Applies to: {section} projects`
- `measure` → `- How to measure: {measure}`
- `enforced` → `- Where enforced:` with sub-items parsed from the field:
  - If contains "Tests" → `  - Tests: yes`; else `  - Tests: none`
  - If contains "CI" → `  - CI: yes`; else `  - CI: none`
- `priority` → `- Priority: {priority}`
- Fixed: `- Current status: proposed` and `- Notes: Auto-generated from NFR catalog ({catalog_id})`

Usage: `nfr-generate.sh --machine --limit 8 | nfr-write-batch.sh`

### 3. ag.sh — Modify `cmd_nfr discover` and `cmd_kickoff`

**File**: `.agentic/lib/tools/ag.sh` (~45 lines changed)

**cmd_nfr discover** (line ~4008):
- Add `--limit 8` to default args passed to `nfr-generate.sh` (user can override with explicit `--limit`)
- Update AGENT INSTRUCTION to reference `nfr-write-batch.sh` for writing selected NFRs
- Tell agent: "looks good" → pipe all through batch writer; "just 1,3,5" → selective; "none" → skip

**cmd_kickoff script mode** (line ~2590, after CONTEXT FILES section):
- Capture `nfr-generate.sh --machine --limit 8` output into a variable
- If non-empty, include it in the AGENT INSTRUCTION as a fenced code block
- Instruct agent: "After `kickoff.py generate` succeeds, save these NFR suggestions to `{staging_dir}/NFR-SUGGESTIONS.md`"
- This satisfies AC-008 (nfr-generate.sh called at ag.sh layer) and AC-009 (content provided for staging)

**cmd_kickoff --review** (line ~2427, after Python pretty-print):
- Check for `NFR-SUGGESTIONS.md` in staging directory
- If exists, display contents with header; if not, degrade silently (no error — covers existing kickoffs without NFR integration)

### 4. Tests

**tests/test_nfr_generate.sh** (MODIFY, ~50 lines added):
- Test `--limit 4` caps output to ≤4 entries
- Test `--limit` prioritizes P1 over P2 (type-specific P1 appears before Universal P1)
- Test `--machine` outputs pipe-delimited format (no ANSI, no header/footer)
- Test `--machine` line count matches human-readable entry count
- Test `--limit` preserves human-readable format (not machine)
- Test `--help` shows `--limit` and `--machine`

**tests/test_nfr_write_batch.sh** (CREATE, ~130 lines):
- Empty stdin → exit 1
- Single entry → creates NFR-0001 with correct format
- Multiple entries → sequential IDs (NFR-0001, NFR-0002, NFR-0003)
- Existing NFR.md with NFR-0003 → starts at NFR-0004
- Statement/category/measure/enforced fields mapped correctly
- Enforced field parsing: "Tests" → Tests: yes, CI: none
- Catalog ID preserved in Notes field

**tests/validate_framework.sh** (MODIFY, ~25 lines):
- F-0216 section:
  - `--limit` flag in nfr-generate.sh help text
  - `--machine` flag in nfr-generate.sh help text
  - `nfr-write-batch.sh` exists and is executable
  - `ag nfr discover` output shows ≤8 entries (grep `nfr-generate.sh.*--limit` in ag.sh)
  - kickoff script mode section contains `nfr-generate.sh` invocation (grep in ag.sh)

### 5. Instruction file updates

- `.agentic/lib/init/init_playbook.md` — Update Step 2c with `--limit 8` and batch writer reference
- `.agentic/lib/init/memory-seed.md` — Update NFR discover trigger section
- `.agentic/lib/DEVELOPER_GUIDE.md` — Update NFR Management section
- `docs/HOW_IT_WORKS.md` — Brief NFR lifecycle note

## Commit Strategy

**Commit 1** (7 files): Core tooling + tests
1. `.agentic/lib/tools/nfr-generate.sh`
2. `.agentic/lib/tools/nfr-write-batch.sh` (new)
3. `.agentic/lib/tools/ag.sh`
4. `tests/test_nfr_generate.sh`
5. `tests/test_nfr_write_batch.sh` (new)
6. `tests/validate_framework.sh`
7. `.agentic/lib/init/init_playbook.md`

**Commit 2** (3 files): Instruction file updates
8. `.agentic/lib/init/memory-seed.md`
9. `.agentic/lib/DEVELOPER_GUIDE.md`
10. `docs/HOW_IT_WORKS.md`

## Verification

1. `bash tests/test_nfr_generate.sh` — all 20 tests pass (14 existing + 6 new)
2. `bash tests/test_nfr_write_batch.sh` — all tests pass
3. `bash .agentic/lib/tools/nfr-generate.sh --project-type web --limit 8` — shows ≤8 entries
4. `bash .agentic/lib/tools/nfr-generate.sh --project-type web --machine --limit 8 | bash .agentic/lib/tools/nfr-write-batch.sh` — writes NFRs to NFR.md
5. `bash tests/validate_framework.sh` — F-0216 section passes
6. Existing tests unchanged: `bash tests/test_nfr_generate.sh` (original 14 pass)

## Review Revisions (from dialectical review)

Changes incorporated from Critic/Advocate feedback:
1. **Replaced awk sorting with section iteration reordering** — simpler, portable, no awk dependency
2. **Specified NFR.md field mapping explicitly** — catalog fields → template format with enforced field parsing
3. **`--limit` applied post-filter** — after priority/component filtering, not pre-filter
4. **Atomic stdin reads in batch writer** — prevents partial writes on pipe errors
5. **Updated `--help` text** — both new flags documented
6. **`--review` degrades silently** when no NFR-SUGGESTIONS.md exists
7. **Kickoff captures machine output to variable** — included in instruction block as fenced code for determinism
